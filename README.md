# Provenance Firewall

Small, dependency-free rules for preventing technical enrichment metadata from leaking into public text. Bring your own rule pack; the defaults are deliberately generic.

```js
sanitizePublicText("provenance: vendor-feed") // null
```

MIT.
